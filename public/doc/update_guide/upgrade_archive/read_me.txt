If you missed previous upgrades than import the following files as per you requirements.
(Must backup your database before importing).

You can ignore 0KB size files - it means no database change in the particular build.